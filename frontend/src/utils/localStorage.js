/*const TOKEN_KEY = 'token';
const USER_NAME_KEY = 'userName';

// Sauvegarde du token dans le localStorage
export const saveToken = (token) => {
  try {
    localStorage.setItem(TOKEN_KEY, token);
  } catch (error) {
    console.error('Error saving token to localStorage', error);
  }
};

// Récupération du token depuis le localStorage
export const getToken = () => {
  try {
    return localStorage.getItem(TOKEN_KEY);
  } catch (error) {
    console.error('Error getting token from localStorage', error);
    return null;
  }
};

// Suppression du token depuis le localStorage
export const removeToken = () => {
  try {
    localStorage.removeItem(TOKEN_KEY);
  } catch (error) {
    console.error('Error removing token from localStorage', error);
  }
};

// Sauvegarde du nom d'utilisateur dans le localStorage
export const saveUserName = (userName) => {
  try {
    localStorage.setItem(USER_NAME_KEY, userName);
  } catch (error) {
    console.error('Error saving user name to localStorage', error);
  }
};

// Récupération du nom d'utilisateur depuis le localStorage
export const getUserName = () => {
  try {
    return localStorage.getItem(USER_NAME_KEY);
  } catch (error) {
    console.error('Error getting user name from localStorage', error);
    return null;
  }
};

// Suppression du nom d'utilisateur depuis le localStorage
export const removeUserName = () => {
  try {
    localStorage.removeItem(USER_NAME_KEY);
  } catch (error) {
    console.error('Error removing user name from localStorage', error);
  }
};*/